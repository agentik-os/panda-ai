# Convex Backend URL
# After running `npx convex dev`, copy the deployment URL here
# Format: https://[your-project].convex.cloud
NEXT_PUBLIC_CONVEX_URL=https://placeholder.convex.cloud

# TODO: Replace with actual URL after Convex initialization
# Run: npx convex dev
# Then copy the URL from the output

# WebSocket Server URL (for real-time updates)
# Development: ws://localhost:8080
# Production: wss://api.agentik-os.com
NEXT_PUBLIC_WS_URL=ws://localhost:8080
